# Auto detect text files and perform LF normalization
* text=auto

# shell 脚本固定 LF，否则在 Unix / Git Bash 下会报错
*.sh text eol=lf

# Windows 批处理固定 CRLF，LF 会让 cmd.exe 解析标签出错
*.cmd text eol=crlf
*.bat text eol=crlf

# PowerShell 脚本固定 CRLF
*.ps1 text eol=crlf
