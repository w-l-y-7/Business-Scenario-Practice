# psmux 使用指南

一句话：psmux 让你的命令行任务**关掉窗口也不停**，还能在一个界面里同时盯好几摊活。

写给金融专业、编程基础薄弱的人（也就是我自己）。

**本文所有命令都在本机实跑验证过。** 实测不支持的几条已单独标明，见 8.7。环境：Windows 11 专业版 + psmux 3.3.8。

> 下面两张速查表看不懂没关系，先扫一眼有个印象。第 5 节会把概念讲清楚，回头看就懂了。

---

## 1. 快捷键速查

psmux 几乎所有操作都要先按一个**前缀键**，再按功能键。

**前缀键 = `Ctrl+B`**

意思就是：先按住 `Ctrl` 再按 `B`，两个键都松开，然后再按后面的功能键。

### 最该记住的五个

| 按键                       | 干什么                                 | 使用频率       |
| -------------------------- | -------------------------------------- | -------------- |
| `Ctrl+B` 然后 `d`      | **离开会话，但让它继续在后台跑** | 天天用         |
| `Ctrl+B` 然后 `c`      | 新建一个窗口                           | 天天用         |
| `Ctrl+B` 然后 `%`      | 把当前屏左右拆成两半                   | 常用           |
| `Ctrl+B` 然后 `方向键` | 在拆开的几块之间移动                   | 常用           |
| `Ctrl+B` 然后 `?`      | 忘了按什么？把所有快捷键列出来         | 想不起来就按它 |

### 完整清单

| 按键                     | 干什么                                                              |
| ------------------------ | ------------------------------------------------------------------- |
| `Ctrl+B` `d`         | 离开会话（**里面的活继续跑**）                                |
| `Ctrl+B` `c`         | 新建窗口                                                            |
| `Ctrl+B` `n` / `p` | 下一个 / 上一个窗口                                                 |
| `Ctrl+B` `0`–`9`  | 直接跳到第几号窗口                                                  |
| `Ctrl+B` `l`         | 回到上一个窗口                                                      |
| `Ctrl+B` `%`         | 左右拆分窗格                                                        |
| `Ctrl+B` `"`         | 上下拆分窗格                                                        |
| `Ctrl+B` `方向键`    | 在窗格之间移动                                                      |
| `Ctrl+B` `z`         | 当前窗格放大 / 还原                                                 |
| `Ctrl+B` `x`         | 关掉当前窗格                                                        |
| `Ctrl+B` `&`         | 关掉当前窗口                                                        |
| `Ctrl+B` `[`         | 进入滚动模式（可以往上翻历史输出）                                  |
| `Ctrl+B` `]`         | 粘贴                                                                |
| `Ctrl+B` `s`         | 列出所有会话 ——**本机不可用**，见 8.7                       |
| `Ctrl+B` `w`         | 列出所有窗口 ——**本机不可用**，见 8.7，改用 `Ctrl+B` 数字 |
| `Ctrl+B` `$`         | 给当前会话改名字                                                    |
| `Ctrl+B` `,`         | 给当前窗口改名字                                                    |
| `Ctrl+B` `:`         | 手动敲命令                                                          |
| `Ctrl+B` `?`         | 查看全部快捷键                                                      |

**滚动模式怎么翻**：按 `Ctrl+B` `[` 进去后，用 `↑` `↓` 或 `PageUp` `PageDown` 翻，按 `q` 退出。

---

## 2. 常用指令速查

快捷键是在界面里用手按；指令是在 PowerShell 里敲。**两者可以混用** —— 比如你在脚本里就可以用指令操作正在跑的会话。

### 会话管理

| 指令                           | 干什么                               |
| ------------------------------ | ------------------------------------ |
| `psmux`                      | 进入默认会话。没有就新建，有就接回去 |
| `psmux new -s 名字`          | 新建一个叫「名字」的会话             |
| `psmux new -s 名字 -d`       | 新建后直接丢后台，不进去             |
| `psmux ls`                   | 列出所有会话                         |
| `psmux attach -t 名字`       | 回到「名字」会话                     |
| `psmux has-session -t 名字`  | 判断会话在不在（在则返回 0）         |
| `psmux kill-session -t 名字` | 关掉「名字」会话                     |
| `psmux kill-server`          | 把所有会话和后台服务全关掉           |

### 窗口和窗格

| 指令                           | 干什么                 |
| ------------------------------ | ---------------------- |
| `psmux new-window -n 名字`   | 当前会话里新开一个窗口 |
| `psmux split-window -h`      | 左右拆分               |
| `psmux split-window -v`      | 上下拆分               |
| `psmux list-windows -t 名字` | 看某个会话里有哪些窗口 |
| `psmux list-panes -a`        | 看所有窗格             |

### 隔空操作（不用进界面就能用）

| 指令                                     | 干什么                                 |
| ---------------------------------------- | -------------------------------------- |
| `psmux send-keys -t 名字 '命令' Enter` | 往那个会话里敲一条命令并回车           |
| `psmux capture-pane -t 名字 -p`        | 把那个会话屏幕上现在显示的内容抓出来看 |

`capture-pane` 很实用：你不用切进界面，直接在 PowerShell 里就能看到后台任务跑到哪了。

### 配置

| 指令                                            | 干什么                       |
| ----------------------------------------------- | ---------------------------- |
| `psmux source-file "C:/Users/王/.psmux.conf"` | 改完配置后重新加载（见 8.6） |

---

## 3. 它是什么、什么时候该用、什么时候别用

### 3.1 它是什么

说人话：**给你的命令行装了个「暂停键」**。

平时你在 PowerShell 里跑一个耗时 40 分钟的活，中途关了窗口，活就没了。psmux 的作用是：让这个活在后台一直跑着，你关窗口、去上课、电脑放着，回来还在。

它还能把一个窗口切成好几块，每块跑不同的活，互不干扰。

技术上说，它是一个 Windows 原生的终端复用器（terminal multiplexer），跟 Linux 上那个著名的 tmux 是同一类工具，不用装 WSL。

### 3.2 什么场景该用它

- **长任务怕断**：跑批量抓数据、批量下载，中途要离开
- **几摊活同时干**：左边跑抓取、右边写报告，不想来回切窗口
- **怕误关**：跑着重要的活，手滑关了窗口

### 3.3 什么场景没必要用

| 场景                   | 为什么不用                         |
| ---------------------- | ---------------------------------- |
| 活几秒钟就跑完         | 直接敲就行，套一层反而麻烦         |
| 你要的是「到点自动跑」 | psmux**不做定时**。见 7.4    |
| 需要图形界面看的东西   | psmux 管的是命令行，不是浏览器窗口 |
| 只是想让命令后台跑一次 | PowerShell 自带`Start-Job`，够用 |

---

## 4. 环境确认

**这台机器已经装好了，不用重装。** 下面是实测结果。

| 项         | 实测情况                                                                               |
| ---------- | -------------------------------------------------------------------------------------- |
| 版本       | psmux 3.3.8                                                                            |
| 安装方式   | WinGet 包`marlocarlo.psmux`                                                          |
| 安装位置   | `C:\Users\王\AppData\Local\Microsoft\WinGet\Packages\marlocarlo.psmux_...\psmux.exe` |
| 别名       | `psmux`、`pmux`、`tmux` **三个名字都能用**，效果完全一样                   |
| 配置文件   | `C:\Users\王\.psmux.conf` —— 已建好，内容见 4.2                                    |
| 默认前缀键 | `Ctrl+B`                                                                             |

三个名字随便用，本文统一写 `psmux`。

### 一个必须先知道的坑

psmux 官方文档说，它默认启动 **PowerShell 7（pwsh）**。

**但你这台机器没装 pwsh。** 实测结果：psmux 会自动退回 **Windows PowerShell 5.1**（实测版本 `5.1.26100.9444`），功能一切正常，不用管。

要记住的是：**以后照官方教程抄命令，凡是写着 `pwsh` 的地方，你换成 `powershell`。**

想确认自己在 psmux 里用的是哪个 PowerShell，进去后敲：

```powershell
$PSVersionTable.PSVersion
```

### 4.2 你的配置文件

路径：`C:\Users\王\.psmux.conf`（已建好）

里面现在有两组设置：

```conf
# 启用鼠标支持（可以用鼠标点击切换面板、拖动调整大小）
set -g mouse on

# 设置面板边框样式，更容易区分不同面板
set -g pane-border-style fg=colour240
set -g pane-active-border-style fg=colour46
```

**想在 PowerShell 里看这个文件**，记得加 `-Encoding UTF8`：

```powershell
Get-Content "C:\Users\王\.psmux.conf" -Encoding UTF8
```

不加的话，PowerShell 5.1 会按系统默认编码去读，中文注释全变成乱码，看着像文件坏了 —— 其实没坏，是读法不对。**实测确认文件本身是合法的 UTF-8**，psmux 也能正常解析。

**实测效果**：

| 设置项         | 改之前             | 改之后                               |
| -------------- | ------------------ | ------------------------------------ |
| 非活动窗格边框 | 默认               | `fg=colour240`（深灰）             |
| 活动窗格边框   | `fg=green`（绿） | `fg=colour46`（亮绿）              |
| 鼠标支持       | `on`             | `on` —— **本来就是默认值** |

那行 `set -g mouse on` 其实是多余的，psmux 出厂就开着鼠标。留着也不碍事，好处是配置换个环境也不会丢这个设置。

`colour240` 和 `colour46` 不是十六进制颜色码，是终端 256 色板的编号。深灰配亮绿，当前光标在哪个窗格一眼就看出来了。

改完配置怎么让它生效，见 8.6。

---

## 5. 三个核心概念

只需要记住三层，像文件夹套文件夹：

| 层                        | 是什么                       | 类比         |
| ------------------------- | ---------------------------- | ------------ |
| **会话**（session） | 最外面一层，一个独立的工作台 | 一个房间     |
| **窗口**（window）  | 会话里的标签页               | 房间里的柜子 |
| **窗格**（pane）    | 窗口切出来的小块             | 柜子里的格子 |

关系是：**一个会话里有多个窗口，一个窗口里能切出多个窗格。**

实际用起来是这样的：

- 你可以开**两个会话**：一个叫 `抓数据`，一个叫 `写报告`。它们是完全隔离的，互不影响
- 在 `抓数据` 会话里开**两个窗口**：一个跑 A 公司，一个跑 B 公司
- 在其中一个窗口里**切两个窗格**：左边跑任务，右边开着 `capture-pane` 看输出

**新手建议**：一开始只会话 + 窗口就够了，窗格等熟练了再用。

---

## 6. 第一个会话：完整走一遍

### 第 1 步：建一个会话

在 PowerShell 里敲：

```powershell
psmux new -s 我的活
```

屏幕会整个变掉，底下出现一条绿色的状态栏，右边写着 `[我的活]`。这就进来了。

### 第 2 步：在里面干点活

随便敲一条耗时的命令试试：

```powershell
1..20 | ForEach-Object { Write-Output "进度 $_"; Start-Sleep -Milliseconds 800 }
```

**实测输出**：屏幕上会一行一行往外吐「进度 1」「进度 2」……

### 第 3 步：离开它（重点）

这就是整套工具的核心动作。

按 `Ctrl+B`，松开，再按 `d`。

你会**回到原来的 PowerShell 提示符**，就好像什么都没发生。

但那个任务还在跑。

### 第 4 步：确认它真的还在

```powershell
psmux ls
```

**实测输出**：

```
0: 1 windows (created Sat Sep 12 19:22:40 2026) (attached)
我的活: 1 windows (created Sat Sep 12 19:51:12 2026)
```

两行会话都在。你那个「我的活」没死。

想看看它跑到哪了，不用进去，直接抓屏：

```powershell
psmux capture-pane -t 我的活 -p
```

**实测**：离开 4 秒后再抓，已经吐到「进度 11」了 —— 它一直在跑。

### 第 5 步：回到那个会话

```powershell
psmux attach -t 我的活
```

回到了刚才离开的地方，输出接着往下走。

### 第 6 步：用完关掉

在里面敲 `exit`，或者在外面敲：

```powershell
psmux kill-session -t 我的活
```

**注意**：`Ctrl+B` `d` 是「离开」，会话还活着；`kill-session` 才是「真的关掉」，里面跑的东西会被终止。别搞混。

---

## 7. 日常金融业务怎么用

这一节是重点。

### 7.1 场景一：长任务挂后台，不用守着

**你的处境**：写了个脚本要抓 30 家上市公司的公告，跑一趟 40 分钟。中间你要去上课、去吃饭。

**怎么用**：

```powershell
psmux new -s 抓公告
# 进了会话后，跑你的脚本
node 抓公告.js
# 按 Ctrl+B 松开再按 d，离开
# 现在可以关掉终端窗口，去干别的
```

回来后：

```powershell
psmux attach -t 抓公告                # 进去看
psmux capture-pane -t 抓公告 -p       # 或者不进界面，直接看跑到哪了
```

**这么用的收益**：脚本不再被你的作息绑架。中途关电脑前记得看一眼它跑完没有。

### 7.2 场景二：一个界面同时盯几摊活

**你的处境**：一边要跑抓取，一边要写报告，还要随时看抓取的输出。

**怎么用**：

```powershell
psmux new -s 课题
```

进去之后：

1. 按 `Ctrl+B` `%` —— 屏幕左右分成两半
2. 左边那半：跑你的抓取脚本
3. 按 `Ctrl+B` `方向键` 跳到右半边
4. 右边那半：开 Claude Code 写报告

想再切一块，就再按一次 `Ctrl+B` `%`。

**这个切分是临时的**，你随时可以按 `Ctrl+B` `x` 关掉多余的块。

### 7.3 场景三：挂住 Claude Code 的长任务

跑 `/loop` 这类会跑很久的东西时，最怕中途被打断。

在 psmux 里跑，离开（`Ctrl+B` `d`）之后它在后台继续。你用 `capture-pane` 随时能看到它进行到哪一步，需要干预了再 `attach` 进去。

### 7.4 一句实话：psmux 不做定时

别指望用它实现「每天早上 9 点自动跑」。

psmux 干的是「**让活一直活着**」，不是「**到点把活叫起来**」。

要定时，得用 Windows 自带的**任务计划程序**（Task Scheduler），让它到点启动一个脚本，脚本里再调 psmux。这是另一码事，需要的话单独讲。

---

## 8. 常见坑

### 8.1 关窗口前忘了 `Ctrl+B d`

如果你直接把终端窗口点掉，psmux 的会话**通常还在**（它是独立的后台服务）。重新打开 PowerShell 敲 `psmux ls` 确认一下。

真全没了，就是敲过 `psmux kill-server`，或者电脑重启导致的。

### 8.2 分不清「离开」和「关掉」

| 你想干的事           | 敲什么                         |
| -------------------- | ------------------------------ |
| 我暂时走开，活继续跑 | `Ctrl+B` `d`               |
| 我要彻底关掉这个会话 | `psmux kill-session -t 名字` |

**搞错的代价**：本来只想走开，结果敲了 `kill-session`，跑了半小时的活全没了。

### 8.3 状态栏看不见了

现象：屏幕底下那条绿色的条没了。

原因通常是：`Ctrl+B` `d` 被你按成了别的组合，或者误进了滚动模式。

按 `Ctrl+B` `[` 进了滚动模式的话，按 `q` 退出来。都不行就 `psmux attach` 重进一次。

### 8.4 官方教程里的 `pwsh` 找不到

见第 4 节那个坑。这台机器没装 PowerShell 7，把 `pwsh` 换成 `powershell`。

### 8.5 会话越开越多

`psmux ls` 一看，五六个会话挂着，早忘了是干嘛的。

**习惯**：建会话时就把名字起清楚（`抓公告`、`写报告`），别用默认的 `0`、`1`。用完随手 `kill-session`。

### 8.6 改了配置，但没生效

这是最容易困惑的一条。

psmux 的配置**只在服务启动时读一次**。你改完 `.psmux.conf`，那个正在跑的服务不会自己知道。

**现象**：改完配置，边框颜色纹丝不动。

**怎么办**，两种：

```powershell
# 办法一：让正在跑的服务重读（推荐，不影响现有会话）
psmux source-file "C:/Users/王/.psmux.conf"
```

```powershell
# 办法二：全部重启（会清掉所有会话，里面的活全没）
psmux kill-server
```

**实测对比**：

| 情况                   | 结果                               |
| ---------------------- | ---------------------------------- |
| 改完配置直接看         | 还是旧的绿边框                     |
| 敲`source-file` 之后 | 立刻变成`colour46`               |
| 新起一个服务           | **自动读取，直接就是新配置** |

所以：**已经跑着的服务要手动重读，之后新开的服务会自动带上。**

顺带一提，路径写成 `C:/Users/王/.psmux.conf` 比 `~/.psmux.conf` 稳。PowerShell 里波浪号不一定会按 psmux 期待的方式展开。

### 8.7 `Ctrl+B w` 按了没反应

**现象**：按 `Ctrl+B` 松开、再按 `w`，什么都不出来。这不是按键姿势的问题。

**原因**：`w` 默认绑在 `choose-window` 这个命令上，而 **psmux 根本没实现它** —— 键绑到了不存在的命令，所以静默失败。`Ctrl+B s` 同理，绑的 `choose-session` 也没实现。

把同类命令逐个试过，结论如下：

| 命令                                 | 结果                                         |
| ------------------------------------ | -------------------------------------------- |
| `choose-window`（`w` 默认绑它）  | 命令不存在                                   |
| `choose-session`（`s` 默认绑它） | 命令不存在                                   |
| `choose-tree`                      | 命令表里有，但绑上去会静默退回默认，等于没用 |
| `display-message`                  | 命令表里有，键也确实触发了，但输出画不出来   |
| `display-menu`                     | **可用**                               |

**怎么办**：

- 切窗口直接按数字：`Ctrl+B` 松开按 `0`–`9`；或 `n` / `p` 前后循环
- 不进去也想看有哪些窗口：`psmux list-windows -t 会话名`
- 想要能上下选的菜单，用实测可用的 `display-menu` 自己绑一个：

```powershell
psmux bind-key -T prefix w display-menu -T 窗口 -x C -y C `
  '0 lead' 0 'select-window -t 0' `
  '1 甲'   1 'select-window -t 1'
```

三个一组：菜单上显示什么、按哪个键选、选中后执行什么。想开机就带上，把这行写进 `.psmux.conf` 再 `source-file` 重读（见 8.6）。

**顺带一个坑**：`join-pane`（把已开着的窗口并进同一屏当窗格）本机也不可靠 —— 命令返回成功，实际只多出一个空窗格，窗口没并进去。想同屏看几摊活，建的时候就按 `Ctrl+B` `%` / `"` 切，别指望事后合并。

---

## 附：实测记录

写这份文档时，在本机跑通的清单：

| 功能                                       | 结果                                     |
| ------------------------------------------ | ---------------------------------------- |
| 新建 / 列出 / 判断存在 / 关闭会话          | 通过                                     |
| 在会话里跑 20 步长任务                     | 通过                                     |
| 离开会话后任务继续跑（4 秒后进度到 11）    | 通过                                     |
| `capture-pane` 不进界面抓屏              | 通过                                     |
| `send-keys` 隔空敲命令                   | 通过                                     |
| 新建窗口、给窗口命名                       | 通过                                     |
| 左右 / 上下拆分窗格                        | 通过                                     |
| `list-windows` / `list-panes` 查看结构 | 通过                                     |
| `psmux` / `pmux` / `tmux` 三个别名   | 通过                                     |
| 确认实际 shell 是 Windows PowerShell 5.1   | 通过（文档说 pwsh，实测退回 5.1）        |
| 新建`.psmux.conf` 并让运行中的服务重读   | 通过（边框 green → colour46）           |
| 全新服务自动加载配置文件                   | 通过（隔离 socket 测试，未影响现有会话） |

**实测不支持的：**

| 功能                                 | 结果                                 |
| ------------------------------------ | ------------------------------------ |
| `choose-window`（`w` 默认绑它）  | 不可用，命令未实现，见 8.7           |
| `choose-session`（`s` 默认绑它） | 不可用，命令未实现，见 8.7           |
| `choose-tree`                      | 不可用，绑上去静默退回默认，见 8.7   |
| `display-message` 从按键触发       | 不可用，键触发了但画不出输出，见 8.7 |
| `join-pane` 合并窗口               | 不可靠，只多出空窗格，见 8.7         |
| `display-menu` 绑窗口菜单          | 可用（8.7 给了写法）                 |
